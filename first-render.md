# First HyperFrames Render

Move into your project:

cd ~/your-project

Render draft:

npm run render -- --quality draft

Or:

npx hyperframes render

Expected result:

output.mp4

If render fails:

- verify Node
- verify HyperFrames skills
- verify ~/.agents/skills
- run npm run check first

